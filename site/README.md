# Scaur site

This directory contains Scaur's dependency-free static product site.

Open `index.html` directly or serve the repository root with any local static
server. Keep relative asset paths intact when publishing the `site/` directory.

The site is a technical proof document: capability claims must match the code,
tests, and protocol documentation in the repository. Do not present planned
adapters or production controls as shipped.
